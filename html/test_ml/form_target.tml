<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Coucou</title>
</head>
<body>
  <h1>Test formulaire</h1>
  <p>
    <{
      if Get. (langage) = "zig" then
        Get.(langage ++ " même en version " ++ version ++ " n'a toujours pas de borrow-checker")
      else
        Get.langage ++ " même en version " ++ Get.version ++ " n'a toujours pas d'argument defer"
    }>
    <{
      let n = 20
    }>
    <{
      (fixfun f n -> if n = 0 then
        ""
      else begin
        Get.langage ++ (f (n-1))
      end) n
    }>
  </p>
</body>
</html>